import * as variables from "./variables.js"

export async function display(value_1, value_2) {
	variables.country.textContent = value_1;
	variables.str = obj.temp;	
    	variables.temp.textContent = `${str}°C`;
}
